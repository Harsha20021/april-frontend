// 22/05/26 -- array -- used to collect data irrespective of the datatype

let arr = [1,2,3,4];
console.log(arr[0]);

// object it is a collection of data that stores key values 
// synatax --  let obj = { name etc }

let obj ={
    name:"David",
};
console.log(obj.name);
console.log(obj[name]);
